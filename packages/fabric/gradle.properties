org.gradle.jvmargs=-Xmx2G -XX:+UseSerialGC
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Minecraft and fabric-api versions are defined per bucket in build.gradle (mcBuckets);
# select a bucket with -PsharedworldMcBucket=<bucket>.
loader_version=0.18.2
loom_version=1.13.6

mod_version=0.1.1
maven_group=link.sharedworld
archives_base_name=sharedworld
